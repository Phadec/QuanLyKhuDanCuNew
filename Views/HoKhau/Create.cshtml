@model QuanLyKhuDanCu.ViewModels.HoKhauViewModel

@{
    ViewData["Title"] = "Tạo hộ khẩu mới";
}

<h1>@ViewData["Title"]</h1>

<div class="row">
    <div class="col-md-8">
        <div class="card mb-4">
            <div class="card-header">
                <i class="fas fa-plus me-1"></i>
                Thông tin hộ khẩu mới
            </div>
            <div class="card-body">
                <form asp-action="Create" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="form-group mb-3">
                        <label asp-for="MaHoKhau" class="form-label"></label>
                        <input asp-for="MaHoKhau" class="form-control" />
                        <span asp-validation-for="MaHoKhau" class="text-danger"></span>
                        <small class="form-text text-muted">Mã hộ khẩu là duy nhất trong hệ thống</small>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="DiaChi" class="form-label"></label>
                        <textarea asp-for="DiaChi" class="form-control" rows="2"></textarea>
                        <span asp-validation-for="DiaChi" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="ChuHoId" class="form-label"></label>
                        <select asp-for="ChuHoId" class="form-control" asp-items="ViewBag.ChuHoId">
                            <option value="">-- Chọn chủ hộ --</option>
                        </select>
                        <span asp-validation-for="ChuHoId" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="GhiChu" class="form-label"></label>
                        <textarea asp-for="GhiChu" class="form-control" rows="3"></textarea>
                        <span asp-validation-for="GhiChu" class="text-danger"></span>
                    </div>

                    <div class="form-group form-check mb-3">
                        <input asp-for="TrangThai" class="form-check-input" />
                        <label asp-for="TrangThai" class="form-check-label"></label>
                    </div>

                    <div class="form-group">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save"></i> Lưu
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="fas fa-arrow-left"></i> Quay lại
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <div class="col-md-4">
        <div class="card mb-4">
            <div class="card-header">
                <i class="fas fa-info-circle me-1"></i>
                Hướng dẫn
            </div>
            <div class="card-body">
                <p>Để tạo hộ khẩu mới, bạn cần:</p>
                <ul>
                    <li>Nhập đầy đủ các thông tin bắt buộc (có dấu *)</li>
                    <li>Mã hộ khẩu nên theo định dạng: <strong>MHK[năm][số thứ tự]</strong>, ví dụ: MHK2023001</li>
                    <li>Chọn đúng chủ hộ từ danh sách người dùng</li>
                </ul>
                <p>Sau khi tạo hộ khẩu, bạn có thể thêm các nhân khẩu vào hộ.</p>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
